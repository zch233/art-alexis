# 主题 1.4.2 / 插件 1.2.2

- See More Work 悬浮/键盘聚焦保留左右细线，不改按钮尺寸。
- 隐藏作品侧栏的重复新增入口，保留列表顶部“新增作品”和新建权限。
- 作品编辑与列表使用“作品图片”，分类继续使用“封面”。
- 暂时隐藏作品图集编辑区，保留图集数据、保存/预览字段和原有前台展示。不执行数据迁移或删除。

备份后先在暂存站上传主题 ZIP，选择替换当前主题；再在插件上传入口上传插件 ZIP 并选择替换。无需先删除。刷新 GoDaddy 缓存和浏览器缓存。若旧上传限制阻止主题 ZIP，可临时切换默认主题再覆盖，完成后重新启用 Art Alexis。正式站先安排维护时段。

回滚：覆盖恢复旧主题/插件 ZIP。本次不改数据库结构。

批量新增建议（尚未实现）：作品列表顶部增加独立按钮；统一选分类，多选图片，每张对应一件作品；预览缩略图并可改标题，默认草稿，确认后发布。显示逐项进度及失败重试，成功项不重复创建。保留单件“新增作品”编辑方式，避免和图集混淆。
