# 本次交付：主题1.3.0 + 作品管理插件1.1.0

先在本地 http://127.0.0.1:9401/ 验收首页和四个分类。右上CONTACT已恢复；未设置邮箱时跳页脚联系区。

## 线上代码更新

先备份并在暂存站操作，验收后才正式更新。两份ZIP都要更新：

1. 先更新作品管理插件1.1.0：包含ZIP误拦截修复及可编辑“详情页标注”。若旧插件阻止ZIP上传，按UPLOAD-FIX.md先SFTP覆盖includes/media.php，再上传完整插件ZIP并替换。
2. 外观→主题→添加→上传新版主题ZIP→替换已安装版本。不要删除活动主题。
3. 清主机缓存/CDN缓存，Ctrl+F5或无痕打开，检查动画、CONTACT、四分类、手机菜单、大图。

## 内容不是主题文件

本轮已按用户要求把源码中的文案、封面和25个展示图项同步到本地WordPress。代码ZIP刻意不含图片/数据库；只上传主题和插件不会自动把这些内容带到线上。

源码含10张明确标注PLACEHOLDER的SVG，已转为安全PNG在本地展示；Current Works也含占位说明，上线前应由客户确认。正式联系邮箱仍待提供。

线上若已有客户新增内容，应做定向内容迁移，不要整库恢复或把暂存数据库覆盖正式站。此次未自动操作任何线上环境。独立VPS方案保留deploy/，内容可通过原私有备份流程迁移。

本地导入前备份在deploy/backups/local-content-2026-09-21T02-15-51-370Z。只回滚代码时恢复主题/插件文件；需要撤销本地内容同步时再使用该备份，注意它会回到导入前数据。
