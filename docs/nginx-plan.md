# 宿主机 Nginx 与内容迁移计划

Full Test Flow：代理TLS、数据库迁移、密码轮换属于高风险。

目标：Git下载代码 → 安装Docker/Nginx → 单独复制私有内容备份 → 新建配置和密码 → 恢复到全新项目 → 安装Nginx模板 → 用户DNS解析 → Certbot免费证书。

范围：生产Compose改为仅WordPress/MySQL常驻；backend固定仅回环绑定、端口可配置；提供HTTP/HTTPS Nginx模板与安全渲染脚本；从当前9401手动导出数据库+媒体，恢复不复制wp-config；只恢复新项目并轮换源站账号凭据。无外部服务器、DNS、证书签发或定时备份操作。保留旧完整备份恢复流程。

自审：代理覆盖客户端X-Forwarded-Proto，避免HTTPS循环；Host限制；上传大小匹配；不默认打开页面缓存；备份停写，恢复拒绝已存在容器/卷；内容迁移明确轮换旧密码及旧管理员权限；图片、备份、证书和密钥均不得入Git。证书续期不是备份定时任务，需上线后配置Certbot定时续期。

验证：域名渲染输入拒绝注入、nginx -t、HTTP/本地测试TLS代理、真实图片和登录、原URL替换、老密码失效、新密码有效、空目标保护、Git审计与原9401回归。真实Let's Encrypt签发/DNS/防火墙上线前验收。

回退：既有9401不变；临时验证单独项目、端口，失败保留数据。新增代码先本地验收，当前请求不自动推送GitHub。
