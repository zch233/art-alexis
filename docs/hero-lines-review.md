# 首屏线条修正验证记录

主题版本 1.4.1；插件仍为 1.2.1。采用开发流程及动效检查：分离加载/入场/完成状态，不改变内容模型；新入场效果仅使用 transform/opacity，尊重减少动画偏好。

检查通过：

- tools/check-hero-lines.cjs：390/1440，加载前无纵线、分段纵线不贯穿标题区、100vw 横线错峰入场、hover 四边框、reduce 关闭动画。
- tools/check-intro-fallback.cjs：脚本失败后首屏恢复可见。
- tools/check-layout.cjs：42 项，320–1920，菜单、长标题、详情、灯箱、无 JS、减少动画。
- tools/check-source-motion.cjs：首屏、联系区、背景和灯箱动效回归。
- tools/compare-home-hover.cjs：源项目与本地的手机/桌面悬浮尺寸和内边距对照通过。
- PHP 8.2：11 个 PHP 文件语法检查通过；git diff --check 通过。

人工查看了初始、入场、完成和卡片悬浮截图。首屏保留参考中的名字右侧内部竖向分隔线，只去除标题及副标题区域多出的外侧纵线。卡片伪元素上绘制四边框避免被悬浮背景覆盖，不改变盒模型尺寸。

首次参考对比因本地 9402 服务未启动失败；启动只读源项目服务后重跑通过。未修改线上，线上缓存和最终显示仍需部署后复查。未声称动画每一帧与 Webflow 完全一致。
