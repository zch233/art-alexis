# 后台精简与 See More Work 边框

Full Test Flow：用户反馈视觉缺陷，后台隐藏涉及已有图集数据完整性。

计划：为 all-work 增加不占尺寸的内描边；仅移除作品侧栏新增子菜单，保留列表按钮和新建权限；作品字段/按钮/列表列名统一为作品图片，分类仍称封面；作品图集编辑区域以 hidden 隐藏，保留现有表单字段、序列化及预览逻辑，避免未提交字段被保存为空。批量新增本轮仅提供方案，不创建数据或改变单件编辑语义。

评审：不改权限、不删除数据、不关闭前台旧图集；JS 原有空元素保护无需变更。hidden 区域保留合法表单序列化。前台边框用 inset shadow 保持原有尺寸。检验编辑器侧栏/顶部按钮/图片选择、分类标签、图集隐藏和保存前后 JSON 一致、前端 hover/focus 的描边及手机布局。

发布：主题 1.4.2，插件 1.2.2，先本地验证和打包，用户暂存验证后更新线上；无需迁移，回滚旧包恢复界面。不新增网络请求或循环，性能影响可忽略。

验证结果：直接 remove_submenu_page 会影响当前编辑账号的路由访问，已改为 CSS 仅隐藏重复快捷入口，保留注册与权限。check-admin-simplify.cjs 通过：菜单/顶部按钮、作品与分类标签、隐藏图集保存前后解析 JSON 完全一致、390/1440 hover/focus 边框。check-admin-filter.cjs 筛选回归通过；PHP 8.2 的11个文件语法通过；ZIP 校验及解压检查通过。测试中修正了 JSON 字符串转义/键顺序比较和页面加载等待、同类链接选择器，不是内容数据差异。
