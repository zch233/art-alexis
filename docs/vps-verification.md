# VPS 配置与仓库整理验收 — 2026-09-11

结论：本地生产配置及手动运维流程验证通过，未部署到真实VPS，未初始化主工程Git、提交或推送GitHub。

## 实际验证

- 候选源码审计：无客户位图、SQL、归档、seed、上传目录或生成密钥；从候选文件复制出的独立目录可运行release构建，不提供私有图片。审计也检查未来仓库已跟踪文件，避免只靠gitignore。
- 构建：普通build不再复制原图；release按允许文件类型导出代码及字体，种子图片不进入发布目录，生成SHA256 manifest。
- PHP：10个插件/主题PHP文件语法检查通过；部署脚本Bash语法通过，Apache配置Syntax OK，Caddy validate通过。
- 生产配置：WordPress7.1 / PHP8.3 Apache / MySQL8 / Caddy；独立项目启动，数据库与网站健康检查通过；仅本机映射9402，数据库不映射端口。
- 浏览器：无图空站正常、私有路径404、编辑登录、插件管理403、创建分类和作品、真实图片上传发布、图片可访问。
- 上传目录放置测试PHP文件，请求403，未执行；正式发布目录不含该测试文件。
- 初始化重复执行未重建账号或重复作品。
- 手动备份：在WSL Ubuntu中实际执行deploy/backup.sh，暂停验证站点、导出数据库与站点归档、生成校验清单并恢复运行。保留完整备份供检查，没有定时任务或备份自动删除策略。
- 手动恢复：实际执行deploy/restore.sh到全新alexis-restore-check1项目，校验通过；原站未改动。隔离恢复站更换本地URL后，作品、原图直链、编辑登录和上传PHP阻断均通过。
- 防覆盖：再次恢复到已有目标返回非零，明确拒绝覆盖。
- 原9401站回归：四分类/四作品、图片、手机布局、编辑登录与权限通过，原始客户内容保留。
- 本地全新核心准备：最初遇到PHP解压器内存不足及长文件名截断，由校验测试发现；改用官方HTTPS下载+系统tar，解压后官方核心校验通过才写完成标记。不修改已存在wp-config的已安装站点。

## 交付与保留

原prepare容器已移除。初始化、CLI和归档工具均按需运行并自动移除，不常驻。验证结束停止并移除本轮额外测试容器，数据卷、备份、原图和代码产物保留；没有批量删除文件或目录。

当前部署说明以vps-deployment.md为准。历史含图ZIP不作为新的源码发布包。

## 尚未验证 / 必须上线验收

真实域名证书签发及续期、VPS防火墙外网可达性、正式SMTP找回密码、异地备份传输、iOS/Android真机、跨地区弱网、并发与大图极限内存。生产测试入口是HTTP本机端口，不代表真实HTTPS验收。没有购买、配置或连接任何外部服务器。
