# 1.3.0 源码动效与详情复刻

采用完整测试流程。权威基准为客户site-package两份HTML及其引用的Webflow交互配置，而不是旧预览或截图推测。

差异清单：目前标题独立上移淡入与原整块hero淡入不同；粒子数量/半径/速度和光晕追踪均被过度简化；滚动背景加深缺失；CONTACT因邮箱空隐藏；详情普通/网格图片标题、hover、灯箱信息与原始结构不同。

执行：读取源页面及交互配置→记录实际动画及完整详情样式→添加行为/样式回归→移植参数并保留WP内容→桌面/手机参考对照、灯箱/返回/分页回归→PHP8.2和独立包验证。新增主题1.3.0，不写线上或客户数据库，不复制示例邮箱、SVG占位作品。

评审：保留GSAP/Lenis/PhotoSwipe，不引入Webflow运行时。按motion技能把全屏背景颜色变化改为固定背景层opacity交叉淡化，避免逐帧改继承变量和全屏重绘；粒子canvas保留原视觉公式但缓存光晕位图，后台页签暂停。按accessibility技能保留减弱动画、灯箱焦点、手机触控。CONTACT始终显示，缺邮箱链接到真实存在的页脚联系区，待用户提供真实邮箱。

测试：首屏整块淡入时长/最终状态、滚动深度层、hover、真实字体、CONTACT空/有邮箱、4分类布局与灯箱、1440/390/768、普通/固定滚动条、无JS和减少动态效果。源模板比本地数据库多作品时不凭空补数据，使用浏览器临时同素材夹具隔离布局差异。

回滚：备份并恢复主题目录，清缓存；不恢复数据库。保留旧包和既有用户修改，不批量删除。线上GoDaddy缓存及真实邮件地址仍需用户验收。

用户随后明确授权“样式和动效，内容全都需要”：范围扩展为本地源码内容同步。执行前已备份数据库和媒体至deploy/backups/local-content-2026-09-21T02-15-51-370Z。导入四分类5/5/5/10张图，共13件后台作品、25个展示图项（按哈希去重存储）；10个SVG占位图转为PNG并保留PLACEHOLDER标记，不当真实作品。4分类封面、标题、首页文案与详情标注按源文件。未导入示例邮箱，不删除原媒体，不碰线上。

为保留原模板首页空标注但详情Mixed media/Now，新增后台可编辑的详情页标注字段，空值继续沿用原阶段标注，插件升1.1.0。导入器仅本地CLI可运行，预检不认识的既有作品即停止，不自动覆盖其他客户作品；再次执行相同来源不产生重复。此次涉及内容回滚时，必须使用上面的导入前私有备份，而不是只恢复主题。
