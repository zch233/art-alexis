# 导出源码还原（替代上一轮视觉近似方案）

## 流程与范围

完整测试流程：涉及首页、分类详情、作品详情的主要浏览路径。
唯一视觉基准为用户提供的 `D:/Users/gupo/Downloads/site-package/index.html` 和 `work-detail.html`。
不修改原始导出目录，不改线上数据库、域名、权限，不推送 Git，不导入占位文案/图片。

## 方案

1. 本地只读展示导出页面，读取原 CSS 的尺寸、断点、字体；记录同视口基线。
2. 清除上一轮近似样式的加载，采用独立、精简的首页与详情样式，保持源稿几何布局。
3. 首页仍使用后台分类、封面焦点、简介、邮箱和排序；不复制示例邮箱。
4. 分类详情使用源码的 880px 画廊、关闭按钮、完整比例图片和 More Work。Current Works 默认双栏，其他分类单栏；保留作品独立链接及图库，分页仍按作品数限量。
5. 作品独立详情沿用同一画廊语言；保留 PhotoSwipe 放大、键盘操作和返回定位。
6. 本地打包验证，然后生成新的主题更新包；VPS 与托管主机共用主题。旧包保留，不覆盖或批量删除。

## 评审

- 简化：不加载 Webflow 的运行时、远程字体和整套无关样式；只迁移相关静态布局，复用现有 GSAP/PhotoSwipe。
- 数据：源码详情包含 SVG 和占位描述，不自动作为客户新作品导入。后台数据始终优先。
- 风险：原移动端固定大字可能溢出；只在实际溢出的小屏边界做缩放，不改正常尺寸的间距。
- 功能：换用完整图片后必须测试分页追加的灯箱与返回定位，不变更现有 REST 接口。
- 部署：只更新主题；保留平台插件、uploads、wp-config 和数据库。已有 ZIP 上传限制使用 SFTP 更新主题规避。

## 测试计划

- 基准：首页 390/1440 同视口比较 hero、标题、ABOUT、WORK、图片、标签盒模型；详情比较列宽、头部、图库、推荐。
- 主路径：分类→灯箱→关闭、分类→作品详情→返回；Current Works 双栏/手机单栏。
- 边界：320/390/768/1024/1440/1920，长标题、空图、无 JS、减少动态效果。
- 失败路径：分页网络失败可重试，图片缺失保留占位，插件未启用给出提示。
- 回归：PHP 8.2 语法、构建、既有内容/权限检查、主题 ZIP 结构及提取校验、Git 素材/秘密审计。
- 回滚：上线前单独备份主题目录；异常时仅恢复主题并清缓存，不恢复数据库。
