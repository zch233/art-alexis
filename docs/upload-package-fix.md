# 安装包误拦截修复计划

完整测试流程：涉及上传安全边界。根因是aa_check_upload挂载全局upload和sideload预处理，管理员也具有aa_manage_content，因此主题和插件ZIP被图片检查拒绝。

方案：仅在wp_handle_upload_prefilter、后台update.php、POST、官方upload-theme/upload-plugin动作、匹配upload_themes/upload_plugins能力、安全nonce以及对应themezip/pluginzip上传临时文件和ZIP扩展名同时成立时交给WP核心安装器校验。不按管理员身份或ZIP扩展名单独放行。保留已有上传错误，sideload和媒体接口不豁免。

自评审：不替代核心ZIP内容检查，不更改作品图片大小/尺寸/WebP安全限制，不改角色、不关闭插件、不动主题/数据库。排除仅靠请求action可伪造的方案；临时文件必须对应官方字段。

执行：先写真实WP过滤器回归测试并观察失败；添加小范围例外；PHP8.2语法及本地Docker WP集成验证；生成插件1.0.1包与单文件修复交付；核对包内无图片和密钥。

测试：主题/插件合法请求、媒体库ZIP、编辑角色、无/错误nonce、错误端点、GET、sideload、错误文件字段、已有上传错误；有效PNG和伪装JPG、超大图片限制不回退。测试仅内存切换用户和请求上下文，不变更数据库或安装主题。

部署：备份线上插件includes/media.php，再通过SFTP覆盖此文件（最小修复）；或备份并覆盖完整插件目录以显示1.0.1版本。暂存先验证真实后台ZIP更新后再正式部署。出现PHP错误或正常图片上传失败即恢复旧文件。无数据库迁移，性能新增少量条件判断，无新外部服务。

结果：改前主题/插件两项失败，改后15项全部通过。测试运行在本地真实WordPress过滤器、能力和nonce环境，不是替代WP函数的mock。PHP8.2的11个文件语法和生成包校验通过。未在正式或暂存站写文件，未执行真实线上安装；由用户覆盖文件后在暂存验收。提交前检查无全局ZIP放行、无编辑者提权、无已有错误清除、无私人数据打包。备份和恢复不涉及数据库。
