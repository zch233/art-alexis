# VPS 交付计划 — 2026-09-11

Full Test Flow：部署、密码、数据备份恢复属于高风险变更。

范围：源码与私有图片分离；无素材构建；可克隆的开发配置；生产 Caddy + Apache WordPress + MySQL；手动初始化、备份及隔离恢复。无定时任务或常驻工具容器。不推送GitHub、不部署外网、不删除文件或数据卷。

步骤：忽略/打包规则 → 可选图片导入 → 手动初始化替代prepare → 生产安全配置 → 手动备份/全新项目恢复 → 语法、无图构建、容器验证及报告。

自审：保留字体、许可证和SVG；客户位图排除。生产仅挂载无图发布产物；密码file secrets；数据库不映射端口；仅Caddy公开。备份停写确保一致性；恢复拒绝已存在数据的目标；不自动删备份。

验证：Git候选无图片/密钥/归档；无素材构建；Compose检查；PHP/shell语法；镜像下载完成后验证入口、幂等安装、备份恢复。域名证书、VPS防火墙、异地备份和性能上线前实测。

回退：保留9401数据，生产验证使用独立项目/端口。仅停止失败项目不删卷；更新前备份，数据升级后不可只回退代码。
