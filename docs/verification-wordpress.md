# WordPress 本地验收记录

日期：2026-09-10 实现，2026-09-11 完成收尾复核。范围：本地功能交付，不代表生产上线验收。

## 已验证

- npm run build：生成主题本地 CSS/JS、字体和初始导入素材。
- node tools/check-php.mjs：PHP 8.3 TOKEN_PARSE 检查 10 个生产 PHP 文件全部通过。
- node tools/motion-check.cjs：真实触控模拟、减少动态效果及桌面三种模式下，滚动后 4 个分类卡片均可见，图片正常加载。补充截图 work/wp-scrolled-*.png。
- tools/integration.php：19 项通过；导入幂等、排序、12 项分页、草稿、隐藏及恢复、回收站关联、引用图片保护和编辑权限。
- tools/browser-check.cjs：5 类页面在 1440、390、320 宽度下检查布局及图片，另含两张真实图片上传、排序、封面、发布、回收和权限检查。passed=true，errors=[]。
- tools/flows-check.cjs：7 项通过，含 12→14 加载不重复、手机返回恢复内容及位置、伪图片/错误 nonce/匿名上传拒绝、不可绕过的内容接口和隐藏后 404。
- tools/preview-check.cjs：4 项通过，含未保存标题及自定义字段预览、已发布内容不变、访客无法访问预览和新草稿预览。
- 大图打开、Esc 关闭；手机导航打开、Esc 关闭。截图保存于 work。

环境：本机 WordPress 7.1、PHP 8.3、SQLite，Edge/Playwright。模拟手机视口不能替代真机测试。

## 自评审与修复

原生预览不自动带入自定义图片字段，已补充当前编辑者专属、30 分钟有效的预览快照，验证未污染已发布内容。隐藏规则覆盖页面和列表接口；图片引用保护包含回收站。普通启动已改为不生成 QA 数据、保留本地密码，该启动调整已作语法检查，尚未完整重启复验。

## 上线前仍须验证

- GoDaddy 实际套餐的 PHP、MySQL/MariaDB、图片解码内存、上传大小和持久磁盘。
- iOS Safari / Android 真机缩放、滑动和弱网体验；大图、并发与跨地区加载性能。
- HTTPS、域名、正式邮箱、客户作品标题与简介、备份及恢复演练。
- 主机页面缓存必须排除受隐藏规则影响的路由，或接入并验证失效机制。当前页面和 API 使用 no-store，未完成生产缓存优化。
- 作品/分类未加入核心 sitemap；隐藏不是文件访问控制，已知原图 URL 仍可访问。

未连接生产服务器、购买服务或配置自动备份。核心升级后可能出现数据库升级提示，应先备份再处理。

## 安装包与测试数据

插件 ZIP 12 个条目，主题 ZIP 14 个条目，均以 art-alexis 为根目录，未包含本地登录资料、运行数据库或 node_modules。30 条本轮 QA 作品逐条验证标题后移至回收站，可恢复，图片文件未删除。测试分类也已移至回收站；原始四件作品保留。
