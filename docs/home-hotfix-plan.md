# 首页二次检查 / 1.2.1

完整测试流程：用户提供了实际浏览器截图，包含固定宽度滚动条、缺失CONTACT、字体回退及边框断裂。

## 已复现及待确认

- 本地1920px + scrollbar-gutter:stable：hero-shell左边262.5，body框线270，相差7.5px。根因为100vw与实际可排版宽度混用。
- 本地邮箱为空时，flex space-between只剩两个元素，导航滑到最右；应保留与CONTACT相同的布局占位但不制造无效联系链接。
- hero-title的伪元素从标题列中点延伸100vw，大屏上达不到右侧边缘；副标题行又多出竖分隔。
- 桌面brand伪元素超出导航高度；需裁切到导航内。
- 公开正式域名读取到旧版主题，并非截图2的新hero结构。字体变细须确认截图对应URL；不能凭推测称线上字体已修复。

## 实施与评审

1. 添加真实字体加载、导航子项位置、滚动条占位、伪元素线段端点的失败回归测试，不只比较父容器。
2. 框架改用百分比基于实际容器宽度；完整横线以整个hero-grid为定位基准，不跟随标题列宽度。
3. 桌面保留不可交互的CONTACT等宽占位，手机不保留占位；限制brand线段高度。
4. 检查字体打包、响应与渲染；增加较小的本地WOFF2、明确的版本化URL与预加载，确保字体随包交付，不改客户字体设计。
5. 无CONTACT/有CONTACT、正常/强制滚动条、390/768/1440/1920等覆盖；详情、灯箱及返回回归；PHP8.2和ZIP验证。
6. 只生成本地主题1.2.1，不写线上。给出同目录覆盖更新，不要求先删除活动主题。

## 边界与回滚

不改插件上传校验、不设置示例邮箱、不改数据、素材、服务器或域名。保留现有脏工作树及旧包。
暂存先验证，正式更新前备份主题和站点；回滚仅恢复主题文件并清缓存，不恢复数据库。
