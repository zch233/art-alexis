# 批量直接发布选项

完整测试流程：涉及发布权限与同项重试。新增默认关闭的原生checkbox，明确即时发布及隐藏分类仍不公开。开始后锁定选择，结果按服务端实际状态显示。

服务端严格校验publish=0/1；发布需要publish_aa_items。始终先建草稿、上传、关联图片，最后发布。记录请求的发布意图，重试不可换意图；图片已关联但发布失败可继续发布；完成标记避免用户后来撤回草稿后旧请求再次发布。旧批次无发布标记按草稿处理。不改变分类公开性，不自动公开隐藏分类。

验证：草稿默认不变，勾选按钮/结果变化，成功publish状态，伪图不能发布，相同key重试同ID/状态，改变意图拒绝，无发布权限拒绝，PHP/JS语法及包完整性。仅本地测试。插件1.3.1，主题不变，暂存替换验证后正式更新，回滚旧插件不撤回已发布作品。

评审：权限在上传前检查；上传失败不发布；同批选项禁用；复用完成函数覆盖成功和已上传重试两条路径。checkbox带label和说明关联；无需弹窗或额外动效。

完成验证：check-bulk-publish.cjs通过默认草稿、直接发布、图片已关联、选项锁定、同ID重试和意图冲突/非法参数/假图拒绝；check-bulk-permission.php在请求内临时过滤能力验证拒绝，无持久角色变化；check-bulk.cjs完整草稿流程及响应丢失重试通过。PHP12文件与JS语法通过，git diff --check通过。无线上操作。
