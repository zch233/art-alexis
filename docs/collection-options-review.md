# 验收与评审

主题1.4.0、插件1.2.0。

- 根因复现：localhost加载127.0.0.1字体触发CORS，Gasoek及Poppins均error；限定loopback资源输出同源URL后两种host加载正常。不开放全站CORS，不改正式域名。
- 分类配置集中在插件helper，合法值白名单；旧分类fallback保留外观，无自动数据库迁移。事务回滚测试覆盖保存、独立选项、重命名、非法值、nonce、权限，不保留测试数据。
- 浏览器8种布局/切换/屏宽组合通过，独立查看不允许键盘换图，关闭恢复焦点；分页仍委托同一个图集入口，新增图片生效。焦点验证等待关闭生命周期结束。
- 原源码卡片/CONTACT/See More Work/导航CONTACT在390及1440普通、hover状态的padding和几何比较通过（2px容差）：work/hover-1789996130976。实际截图检查边线、圆角、箭头；CONTACT复用源SVG，未引入远程图标依赖。真实邮箱缺失提示与源占位邮箱内容不同是有意保留。
- 四分类×三屏宽的12组详情样式/几何零差异：work/detail-styles-1789996181790。动效/灯箱检查：work/source-motion-check-1789996171422。9项详情/分页回归通过。
- PHP8.2全部11文件通过，Git候选152文件无私有图片或密钥，代码独立构建通过；ZIP路径/哈希/PHP解压检查通过。

范围：未操作线上、未推送Git、未重导内容；主题和插件一起更新，新增字段旧版本忽略。切换只包含已加载的图片。源源码的短时hover padding按用户要求保留，不新增滚动布局循环；没有宣称完成真实性能基准测试。

发布/回滚见deploy-managed/UPDATE-1.4.0.md，先暂存验收，异常时恢复先前代码而非整库覆盖客户内容。
